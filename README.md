# Prelegal

> **Work in Progress** — This project is currently under active development and is expected to be completed within 1 week.

## Status

This project is not yet ready for production use. Check back soon for updates.

  To run the app now on port 8000:

  cd backend && uv run uvicorn main:app --port 8000
  cd frontend && npm run dev   # http://localhost:3000